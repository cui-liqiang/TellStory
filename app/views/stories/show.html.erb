<div class="container">
    <div class="left">
        <div class="story-content">
            <h3 class="story-title"><%= @story.title%></h3>
            <ul>
                <li>
                    <%= @story.content %>
                </li>
                <% @story.adopted_follows.each do |follows|%>
                    <li>
                        <p><%= follows.content%></p>
                    </li>
                <% end %>
            </ul>
            <div class="social"></div>
            <textarea class="add-content" name="follow"></textarea>
            <br>
            <input id="tokentag" type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />
            <a class="submit-content" href="#">提交续写</a>
        </div>

    </div>

    <div class="right">
        <div class="story-added">
            <h3 class="chapter-title">所有本章节续写</h3>
            <ul>
                <% @story.current_follows.each do |follow|%>
                    <li class="vote_wrap">
                        <p><%= follow.content%></p>
                        <div><span>投票</span>
                        <span class="vote">
                            <input type="hidden" value="<%= follow.id%>" />
                            <span><%= follow.votes%></span>
                        </span></div>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</div>